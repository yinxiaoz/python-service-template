version: "3.8"

services:
  python-service-template:
    build:
      context: ../..
      dockerfile: Dockerfile
    ports:
      - "5001:5000"
    environment:
      - HOST=0.0.0.0
      - PORT=5000
      - LOG_LEVEL=INFO
    volumes:
      - ../../src:/app/src:ro
    restart: unless-stopped
